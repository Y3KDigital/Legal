# Compliance Engine (Structured)

This folder is intended to be machine-readable.

- `state_machines/` — lifecycle definitions and gates
- `controls/` — reusable controls (KYC/AML, sanctions, disclosures)
- `policy_packs/` — per-jurisdiction rules and overrides

Nothing in this folder should be “hand-wavy” prose; use `docs/` for narrative.
